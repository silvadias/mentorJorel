info:
  name: payloadRequest
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/initial-chat"
  body:
    type: json
    data: |-
      {
        "prompt": "então vamos começar o teste, a requisição que eta recebendo mantem histórico eu preciso enviar, estou enviando pelo body do meu express quais argumentos preciso enviar, se for um outro usuário por exemplo. so estou enviando o prompt na requisição, o que mais posso enviar"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
